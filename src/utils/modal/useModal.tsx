// import { useOverlay } from '../overlay';

export const useModal = () => {
  //   const overlay = useOverlay();
  //   const openModal = () =>
  //     new Promise(resolve =>
  //       overlay.open(({ isOpen })))
};
